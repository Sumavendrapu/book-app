apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: book-app
  template:
    metadata:
      labels:
        app: book-app
    spec:
      containers:
      - name: book-app
        image: sumavendrapu/book-app:6868940ef6b0b29d7aa244750ea1226b0ba357aa
        imagePullPolicy: Always
        ports:
        - containerPort: 8000

        envFrom:
        - configMapRef:
            name: book-app-config

        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: book-app-secret
              key: DB_PASSWORD

        volumeMounts:
        - name: app-storage
          mountPath: /data

      volumes:
      - name: app-storage
        persistentVolumeClaim:
          claimName: mysql-pvc